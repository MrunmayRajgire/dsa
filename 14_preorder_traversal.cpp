/*
#include <bits/stdc++.h>
using namespace std;

void preorder(node)
{
    if(node == NULL)
    {
        return;
    }
    cout<<node->data;
    preorder(node->left);
    preorder(node->right);
}
*/